[project]
name = "autobio-inference"
version = "0.1.0"
requires-python = ">=3.7"
dependencies = [
    "msgpack>=1.0.5",
    "numpy>=1.21.6",
    "websockets>=11.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
